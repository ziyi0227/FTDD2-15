<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ftdd2.mapper.MenuTableMapper">

    <select id="getMenuListByUserId" resultType="com.ftdd2.domain.entity.MenuTable">
        SELECT distinct a.*
        FROM menu_table a,
             role_menu b,
             user_role c
        WHERE a.`menu_id` = b.`menu_id`
          AND b.`role_id` = c.`role_id`
          AND a.`parent_id` = #{pid}
          AND c.`user_id` = #{userId}
    </select>
</mapper>
